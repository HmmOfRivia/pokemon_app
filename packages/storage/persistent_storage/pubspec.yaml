name: persistent_storage
description: Saves the data in a device's persistent memory.
version: 0.0.1
publish_to: none

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.15
  storage:
    path: ../storage_interface

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
